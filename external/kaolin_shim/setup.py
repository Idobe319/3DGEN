from setuptools import setup, find_packages

setup(
    name='kaolin-shim',
    version='0.0.1',
    description='Kaolin shim for local testing',
    packages=find_packages(),
    include_package_data=True,
)
